import{rm,mkdir,copyFile,cp}from'node:fs/promises';await rm('dist',{recursive:true,force:true});await mkdir('dist',{recursive:true});for(const f of['index.html','gps.css','gps-config.js','gps.js'])await copyFile(f,'dist/'+f);await cp('labo1','dist/labo1',{recursive:true});console.log('Build Nexus.Tutor GPS + Labo1 completo.');