import{rm,mkdir,copyFile,cp}from'node:fs/promises';await rm('dist',{recursive:true,force:true});await mkdir('dist',{recursive:true});for(const f of['index.html','gps.css','gps-config.js','gps.js','tutor.html','tutor.css','tutor-health.css','tutor-config.js','pickup-time-contract.js','tutor-observability.js','tutor.js'])await copyFile(f,'dist/'+f);await cp('labo1','dist/labo1',{recursive:true});console.log('Build Nexus.Tutor GPS + Experiencia Tutor + Labo1 completo.');